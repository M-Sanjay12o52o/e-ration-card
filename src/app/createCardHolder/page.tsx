import CreateCardHolder from '@/components/CreateCardHolder'
import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        <CreateCardHolder />
    </div>
}

export default page